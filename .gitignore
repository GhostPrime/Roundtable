node_modules
dist
release
.DS_Store
*.log

# Local env / secrets / personal notes — never commit
.env
.env.*
*.local
*.local.md
git_broken/

# AI tooling config — local to this machine, may hold model/agent settings
# and permissions. Never publish.
.claude/
.cursor/
.aider*
CLAUDE.md

# Internal handoff docs — kept locally as reference, not published to the repo.
HANDOFF_*.md

# Mobile sister app lives on the mobile-app branch — keep it out of this one.
# (Ignore only affects untracked files, so mobile-app's tracked copy is safe.)
mobile/
